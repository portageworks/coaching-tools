{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn app:app --worker-class gevent --workers 2 --timeout 300",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
